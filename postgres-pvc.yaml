apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namesapce: filterfacts
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nks-block-storage